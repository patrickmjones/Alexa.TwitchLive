/* Update configuration accordingly */
var config = {
    // Change to Lambda application id
    AppID:      "",

    // Change to Twitch client id
    ClientID:   "",

    // Change to Twitch username
    UserID:     ""   
}

module.exports = exports = config;
